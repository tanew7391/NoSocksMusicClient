block variables
doctype html
html
  head
    block head
      meta(charset="utf-8")
      link(rel="shortcut icon", href="/favicon.ico")
      meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
      meta(name="theme-color", content="#000000")
      title #{title || 'Spotify Info'}
      link(rel="stylesheet" href="/style.css")
  body
    block body-header
      include body-header
    block body-content
      h1 Hey dingus!
      h2 You forgot to add body content.
    block body-header
      include body-footer
    block scripts